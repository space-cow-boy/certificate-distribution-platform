{
  "name": "certificate-distribution-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:vercel": "npm ci --include=dev --prefix frontend && npm run build --prefix frontend && node scripts/vercel-copy-dist.mjs"
  }
}
